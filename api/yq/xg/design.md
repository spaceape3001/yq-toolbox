#! eXecutive Graph

Executive graph is meant as a transition network style of execution flow; similar to flow charts, but hopefully easier for people to grasp.

## Flow

### Always

Always is basically an interrupt, so it'll be queried, if triggered, it'll activate.  Higher priority wins.

AlwaysXG will always start.

### Start

These are triggers to start a graph.  StartXG will always start.


